name: Apply Sonar hotspot fix

on:
  push:
    branches:
      - codex/security-hotspots

permissions:
  contents: write

jobs:
  fix:
    if: github.event.head_commit.message == 'Run Sonar hotspot patch'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: codex/security-hotspots
          fetch-depth: 0
      - name: Apply patch
        run: python scripts/fix-sonar-hotspots.py
      - name: Commit source change
        run: |
          git config user.name "Stanley Lloyd"
          git config user.email "131160519+StanleyLl0yd@users.noreply.github.com"
          git add src/Settings.cpp
          git commit -m "Harden registry string writes"
          git push origin HEAD:codex/security-hotspots
