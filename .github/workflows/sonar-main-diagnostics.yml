name: Sonar main diagnostics

on:
  push:
    branches:
      - codex/main-sonar-fixes

permissions:
  contents: read

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - name: Fetch Sonar main findings
        shell: bash
        run: |
          set -euo pipefail
          curl --fail --silent --show-error --get \
            'https://sonarcloud.io/api/issues/search' \
            --data-urlencode 'componentKeys=StanleyLl0yd_everon' \
            --data-urlencode 'branch=main' \
            --data-urlencode 'resolved=false' \
            --data-urlencode 'ps=100' \
            | jq '{total, issues: [.issues[] | {key, rule, severity, type, component, line, message, impacts}]}'
